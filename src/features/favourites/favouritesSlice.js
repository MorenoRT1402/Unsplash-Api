import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    images: []
}

export const favouritesSlice = createSlice({
    name: 'favourites',
    initialState,
    reducers: {

    }
})